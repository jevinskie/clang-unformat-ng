#pragma once

#include "common.hpp"

#include "core.hpp"

#include <cstdint>
#include <string>
#include <vector>

namespace unformat {
std::string slurp_file_string(const std::string &path);
}; // namespace unformat
